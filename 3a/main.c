#include <stdio.h>
#include <stdlib.h>
#include "main.h"
#include "lib.h"

int main()
{
    int option, n=0;
    int n = input(&size);
    //проверки
    Table* table = init(size);
    while(option = dialog()); // пока option = dialog не 0
    {
        // массив функций

    }
    return 0;
}